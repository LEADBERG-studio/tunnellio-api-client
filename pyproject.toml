[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tunnellio"
version = "0.6.3"
description = "Self-sufficient CLI for planning and launching Tunnellio tunnels (SSH and native TCP bridge)"
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Tunnellio" }
]
dependencies = [
  "truststore>=0.9.1"
]

[project.scripts]
tunnellio = "tunnellio.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
